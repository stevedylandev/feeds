version: '3.8'

services:
  feeds:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/usr/src/app/data
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
